/**
 * @jest-environment node
 */

import { createTestRouter } from "../../../utils/testing";

describe("test explore router", () => {
  const USER: any = { id: "tester_id", name: "tester" };

  it("happy path for explore.cards route", async () => {
    // const router = createTestRouter(USER);
  });
});
